services:
  - type: web
    name: two-servers
    env: node
    buildCommand: ./build.sh
    startCommand: npm run start:render
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production